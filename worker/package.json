{
  "name": "unique-code-generator-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create unique-code-generator-db",
    "db:migrate:local": "wrangler d1 migrations apply unique-code-generator-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply unique-code-generator-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
